📘 АНАЛИЗ ОТЧЁТА ПО ЛАБОРАТОРНОЙ РАБОТЕ №13
Тема: наследование и полиморфизм. Цель: изучение принципов ООП и применение инструментов разработки (IDE, Git, тестирование).

1. Анализ структуры программы
Разработанная программа включает иерархию классов, отражающую структуру учебного заведения. Была реализована базовая система наследования, состоящая из четырёх классов:

1.1. Базовый класс Person
Класс содержит общие атрибуты (name, age) и метод display_info(). Он представляет собой универсальный шаблон, от которого наследуются остальные типы персонала. Благодаря этому достигается устранение дублирования кода.

1.2. Класс Student
Наследует Person и расширяет его функциональность, добавляя:

учебную группу,
средний балл (GPA).
Метод display_info() переопределён для отображения специфичных данных. Это демонстрирует принцип переопределения (overriding) и расширения базового класса.

1.3. Класс Teacher
Добавляет:

предмет преподавания,
стаж работы.
Функционально аналогичен Student, но со своей спецификой. Это показывает способность классов наследовать общее поведение, но давать собственную реализацию методов.

1.4. Класс AdminStaff
Добавляет:

должность,
отдел.
Также переопределяет метод display_info().

Таким образом, все три дочерних класса обладают общим интерфейсом, но реализуют его по-разному.

2. Анализ реализации принципов ООП
2.1. Наследование
Реализовано корректно: все дочерние классы используют super() для обращения к конструктору родителя. Это обеспечивает:

компактность кода,
расширяемость,
избегание повторения общих атрибутов.
2.2. Инкапсуляция
Атрибуты хранятся внутри объектов и обрабатываются методами. Хотя не используются модификаторы доступа (_, __), базовая инкапсуляция присутствует.

2.3. Полиморфизм
Явно демонстрируется в блоке:

python people = [Student(...), Teacher(...), AdminStaff(...)] for p in people: print(p.display_info())

Все объекты вызываются единообразно, но каждый отвечает по-разному. Это пример позднего связывания и динамического полиморфизма.

2.4. Переопределение методов
Каждый дочерний класс имеет собственную версию display_info(). Это наглядно показывает возможности модификации поведения родительских классов.

3. Анализ юнит-тестирования
Файл test_person.py использует модуль unittest, включающий:

проверку диапазона gpa,
проверку корректности опыта преподавателя,
проверку сохранения атрибутов отдела,
тест на строку, возвращаемую методом display_info().
Тесты обеспечивают:

контроль корректности хранения данных,
контроль работы методов.
Тестовое покрытие базовое, но достаточное для учебной задачи.

4. Анализ инструментов разработки
Работа предполагает использование IDE (VS Code или PyCharm). Это позволяет:

запускать программу с точки входа,
отслеживать переменные,
отлаживать ошибки.
Также подразумевается размещение проекта в Git-репозитории. Структура папок соответствует требованиям: main.py, test_person.py, README.md, requirements.txt.

Это правильно организованный подход к учебному проекту.

5. Итоговый вывод
Выполненная работа полностью соответствует требованиям лабораторной работы №13:

✔ реализована иерархия классов ✔ применено наследование и полиморфизм ✔ методы корректно переопределены ✔ продемонстрировано динамическое связывание ✔ написаны юнит-тесты ✔ структура проекта соответствует рекомендациям ✔ отчёт оформлен в виде README

Программа содержит чистый, читаемый код и хорошо демонстрирует возможности объектно-ориентированного программирования на Python.

